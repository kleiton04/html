pipeline {
  agent any
  stages {
    stage('Build') {
      steps {
        sh 'ls -l'
        sh 'echo "Iniciando o Build"'
      }
    }
    stage('Test') {
      steps {
        sh 'pwd'
      }
    }
  }
}